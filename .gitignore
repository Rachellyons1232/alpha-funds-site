# macOS
.DS_Store

# Local Claude config
.claude/

# Large binaries / tools (not source)
ffmpeg

# Build artifacts / deploy bundles (regenerated, no need in git)
alpha-funds-deploy/
alpha-funds-deploy.zip
export/

# Video files (not referenced by the site)
*.mp4
video/

# Frame sequences not used by the site
frames-cosmic/

# Unused / source PNGs (kept locally, not pushed)
sophia space image.png
sophia-space-duotone.png
Screen Shot*.png

# Private brand documents
*.pdf
