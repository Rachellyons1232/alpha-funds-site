# macOS metadata
.DS_Store
**/.DS_Store

# Local Claude Code config (per-machine, not part of the site)
.claude/

# Tool binary — not site content, can be re-downloaded if needed
ffmpeg

# Zip archives that just duplicate tracked folders
*.zip
